# 第五章 安装及使用

## 5.1 安装环境要求

为确保工程图纸智能管理系统正常运行，需满足以下环境要求：

| 项目 | 版本/要求 | 说明 |
|------|-----------|------|
| 操作系统 | Windows/Linux/MacOS | 支持主流操作系统 |
| Python | 3.9+ | 推荐使用Python 3.9.13 |
| 内存 | 2GB+ | 建议4GB以上以获得更好性能 |
| 磁盘空间 | 1GB+ | 用于安装依赖和存储上传的图纸 |
| 网络连接 | 稳定 | 用于调用AI模型API和下载依赖 |

## 5.2 安装过程

### 5.2.1 基础安装

1. **克隆或下载项目代码**
   - 将项目代码复制到本地目录

2. **运行安装脚本**
   - **Windows系统**：双击运行 `install.bat` 文件
   - **Linux/MacOS系统**：执行 `chmod +x install.sh && ./install.sh`

   安装脚本会自动执行以下操作：
   - 检查Python环境
   - 升级pip
   - 安装Python依赖
   - 安装PaddlePaddle
   - 安装PaddleOCR

3. **安装Poppler**
   - 下载地址：https://github.com/oschwartz10612/poppler-windows/releases/
   - 将poppler的bin目录添加到系统环境变量PATH

4. **配置API密钥**
   - 打开 `main.py` 文件
   - 修改以下API密钥配置：

   ```python
   # DeepSeek API配置
   DEEPSEEK_API_KEY = "你的DeepSeek API密钥"

   # 千问API配置
   QWEN_API_KEY = "你的千问API密钥"
   QWEN_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"
   ```

### 5.2.2 依赖项说明

系统主要依赖项及其版本：

| 依赖项 | 版本 | 用途 |
|--------|------|------|
| fastapi | 0.128.8 | Web框架 |
| uvicorn | 0.39.0 | ASGI服务器 |
| opencv-python | 4.6.0.66 | 图像处理 |
| numpy | 1.21.6 | 数值计算 |
| pillow | 11.3.0 | 图像处理 |
| pdf2image | 1.17.0 | PDF转图片 |
| paddlepaddle | 2.6.2 | 深度学习框架 |
| paddleocr | 2.6.1.3 | OCR识别 |
| requests | 2.32.5 | HTTP请求 |
| jinja2 | - | 模板引擎 |

## 5.3 系统启动

### 5.3.1 启动命令

在项目根目录执行以下命令启动系统：

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 5.3.2 访问地址

启动成功后，可通过以下地址访问系统：

- **主页**：http://localhost:8000/home
- **API文档**：http://localhost:8000/docs
- **系统状态**：http://localhost:8000/系统状态

## 5.4 典型使用流程

### 5.4.1 上传图纸

1. **访问系统主页**：打开浏览器访问 http://localhost:8000/home
2. **选择文件**：点击"选择文件"按钮，或直接拖放PDF/PNG文件到上传区域
3. **上传图纸**：点击"上传图纸"按钮
4. **等待处理**：系统会自动进行OCR识别和分析
5. **查看结果**：上传完成后，图纸会显示在图纸列表中

### 5.4.2 使用智能助手

1. **选择模型**：在主页的"智能助手"区域选择模型（千问VL或DeepSeek）
2. **输入问题**：在输入框中输入关于图纸的问题
3. **发送请求**：点击"发送"按钮
4. **查看回答**：系统会显示AI助手的回答
5. **拖放分析**：也可以直接拖放PDF文件到对话框进行分析

### 5.4.3 查看OCR结果

1. **找到图纸**：在图纸列表中找到需要查看的图纸
2. **点击查看**：点击图纸右侧的"查看OCR"链接
3. **查看详情**：系统会显示标题栏、技术要求和全局OCR结果
4. **导出结果**：可以点击"导出OCR"按钮将结果导出为文本文件

### 5.4.4 搜索图纸

1. **输入关键词**：在主页的搜索框中输入关键词
2. **点击搜索**：点击"搜索"按钮
3. **查看结果**：系统会显示符合条件的图纸列表
4. **排序结果**：可以选择按上传时间正序或倒序排列

## 5.5 常见问题及解决方案

### 5.5.1 上传失败

**问题**：上传图纸时失败

**解决方案**：
- 检查文件格式是否为PDF或PNG
- 检查文件大小是否超过50MB限制
- 检查网络连接是否正常

### 5.5.2 OCR识别失败

**问题**：OCR识别失败或结果不准确

**解决方案**：
- 检查API密钥是否正确配置
- 检查网络连接是否正常
- 尝试使用不同的模型（千问VL或PaddleOCR）
- 确保图纸清晰，避免模糊或倾斜

### 5.5.3 服务器启动失败

**问题**：服务器启动失败

**解决方案**：
- 检查8000端口是否被占用
- 检查依赖是否安装完整
- 检查Python版本是否为3.9+
- 检查API密钥是否正确配置

### 5.5.4 系统运行缓慢

**问题**：系统运行缓慢

**解决方案**：
- 关闭其他占用系统资源的程序
- 增加系统内存
- 优化网络连接
- 对于大型图纸，可能需要更长的处理时间

## 5.6 系统维护

### 5.6.1 数据库管理

- **查看数据库**：系统使用SQLite数据库，文件为 `database.db`
- **备份数据库**：定期备份 `database.db` 文件
- **清理数据**：可通过"删除所有图纸"功能清理数据库和文件

### 5.6.2 日志管理

- 系统会在控制台输出运行日志
- 可根据日志信息排查问题

### 5.6.3 版本更新

- 当需要更新系统时，替换相应文件即可
- 注意备份数据库和配置文件

## 5.7 性能优化建议

1. **硬件优化**
   - 使用SSD存储以提高文件读写速度
   - 增加内存以提高图像处理性能

2. **软件优化**
   - 定期清理上传目录，删除不需要的文件
   - 优化API调用频率，避免频繁请求

3. **使用建议**
   - 对于大型图纸，建议分批处理
   - 使用千问VL模型获得更准确的OCR结果
   - 合理设置PDF转图片的DPI值（默认400）

通过以上步骤，您可以成功安装和使用工程图纸智能管理系统，享受AI技术带来的高效图纸管理体验。
